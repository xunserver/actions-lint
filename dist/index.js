const { getInput } = require("@actions/core")

const inputs = {
    binPath: getInput('bin_path'),
    ext: getInput('ext'),
    configPath: getInput('config_file'),
}

const run = async () => {
    try {
        
    }catch(err) {
        
    }
}

run()